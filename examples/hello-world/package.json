{
  "type": "module",
  "devDependencies": {
    "prettier": "^3.2.5",
    "typescript": "^5.4.5",
    "vite": "^5.2.12"
  },
  "scripts": {
    "dev": "zig build && vite",
    "build": "zig build && vite build"
  }
}
